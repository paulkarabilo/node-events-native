{
  "name": "native-events",
  "version": "0.0.1",
  "description": "native event emmiter for node (experimental)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pk",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "bindings": "^1.3.0",
    "nan": "^2.7.0"
  }
}
